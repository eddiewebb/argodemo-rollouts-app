---
# Source: apps-template/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: svc-stable
  annotations:
    prometheus.io/fastscrape: 'true'
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: 'eddie-http-bin-app'
---
# Source: apps-template/templates/rollout.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "eddie-http-bin-app"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "eddie-http-bin-app"
  template:
    metadata:
      labels:
        app: "eddie-http-bin-app"
    spec:
      containers:
        - image: "kennethreitz/httpbin:latest"
          name: "eddie-http-bin-app"
          ports:
            - containerPort: 8080
          env:
            - name: SEMVER
              value: "latest"
            - name: STAGE
              value: dev
            - name: NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
  minReadySeconds: 30
  revisionHistoryLimit: 3
---
# Source: apps-template/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stable-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: "dev.http-bin.akpdemoapps.link"
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: svc-stable
            port:
              number: 80
