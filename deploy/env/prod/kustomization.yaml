apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base

namespace: demo-prod

labels:
  - pairs:
      rollouts-app/stage: "prod"
    includeSelectors: true  # Apply label to selectors (e.g., Deployment's selector)
    includeTemplates: true   # Apply label to po